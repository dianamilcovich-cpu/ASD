#ifndef ASD_BARRIER_HPP
#define ASD_BARRIER_HPP

#include "../../struct/struct.h"
#include <vector>

vector<int> bmh_search_persons(const vector<person>& data, const vector<string>& pattern_m, int requiredCount);

#endif //ASD_BARRIER_HPP
